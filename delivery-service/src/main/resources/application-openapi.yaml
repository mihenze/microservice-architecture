spring:
  cloud:
    stream:
      enabled: false

  kafka:
    bootstrap-servers: dummy:1234

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration